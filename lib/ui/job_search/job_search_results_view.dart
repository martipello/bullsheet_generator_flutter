import 'package:flutter/material.dart';

class JobSearchResultsView extends StatelessWidget {
  const JobSearchResultsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
